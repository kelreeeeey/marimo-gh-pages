<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>marimo WebAssembly + GitHub Pages Template</title>
  <!-- Include Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans text-gray-800 bg-white p-5">    
  <div class="max-w-4xl mx-auto">
    <!-- Header -->
    <header class="bg-gray-100 p-6 text-center rounded-lg mb-6">
      <!-- <img src="https://raw.githubusercontent.com/marimo-team/marimo/main/docs/_static/marimo-logotype-thick.svg" alt="marimo Logo" class="w-20 h-auto mx-auto mb-3"> -->
      <h1 class="text-2xl font-bold mb-2">Pyhton x Marimo - WebAssembly Notebooks</h1>
      <p class="text-sm">A collection of things you probably like</p>
    </header>

    <main>
      {% if notebooks %}
      <h2 class="text-xl font-bold text-left my-4">Regular Notebooks</h2>
      <p class="text-center mb-4">Interactive notebooks in edit mode - you can modify and experiment with the code</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
        {% for notebook in notebooks %}
        <!-- {{ notebook.display_name }} Notebook -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-200 p-3 font-semibold">{{ notebook.display_name }}</div>
          <div class="p-4">
            <a href="{{ notebook.html_path }}" class="inline-block bg-blue-500 hover:bg-blue-600 text-white py-1 px-3 rounded transition-colors">Open Notebook</a>
          </div>
        </div>
        {% endfor %}
      </div>
      {% endif %}

      {% if apps %}
      <h2 class="text-xl font-bold text-left my-4">Notebook Apps</h2>
      <p class="text-center mb-4">Interactive applications in run mode - code is hidden for a clean user interface</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {% for app in apps %}
        <!-- {{ app.display_name }} App -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-100 p-3 font-semibold">{{ app.display_name }}</div>
          <div class="p-4">
            <a href="{{ app.html_path }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Open App</a>
          </div>
        </div>
        {% endfor %}
      </div>
      {% endif %}

      {% if petrophysics_app %}
      <h2 class="text-xl font-bold text-left my-4">Interactive Petrophysics Notebooks: A Modification of McDonald, A., (2021), "Python and Petrophysics Notebook Series"</h2>
      <p class="text-left mb-5"><a href="https://github.com/andymcdgeo/Petrophysics-Python-Series">Link to Original Repository</a></p>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {% for app in petrophysics_app %}
        <!-- {{ app.display_name }} App -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-100 p-3 font-semibold">{{ app.display_name }}</div>
          <div class="p-4">
            <a href="{{ app.html_path }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Open App</a>
          </div>
        </div>
        {% endfor %}
      </div>
      {% endif %}

      {% if seismic_app %}
      <h2 class="text-xl font-bold text-left my-4">Seismic Data Playground</h2>
      <p class="text-center mb-4">I don't know with this one, they will be soo dang slow since the data are soo big.</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {% for app in seismic_app %}
        <!-- {{ app.display_name }} App -->
        <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-100 p-3 font-semibold">{{ app.display_name }}</div>
          <div class="p-4">
            <a href="{{ app.html_path }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Open App</a>
          </div>
        </div>
        {% endfor %}
      </div>
      {% endif %}

      <!-- {% if collections %} -->
      <!-- <h2 class="text-xl font-bold text-center my-4">Collections of Notebook Apps</h2> -->
      <!-- <p class="text-center mb-4">Collections of Notebook</p> -->
      <!-- <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"> -->
      <!--   {% for collection in collections %} -->
      <!--   <!-- {{ app.display_name }} App --> -->
      <!--   <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden"> -->
      <!--     <div class="bg-gray-100 p-3 font-semibold">{{ collection.display_name }}</div> -->
      <!--         <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"> -->
      <!--           {% for app in apps %} -->
      <!--           <!-- {{ app.display_name }} App --> -->
      <!--           <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden"> -->
      <!--             <div class="bg-gray-100 p-3 font-semibold">{{ app.display_name }}</div> -->
      <!--             <div class="p-4"> -->
      <!--               <a href="{{ app.html_path }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Open App</a> -->
      <!--             </div> -->
      <!--           </div> -->
      <!--           {% endfor %} -->
      <!--         </div> -->
      <!--   </div> -->
      <!--   {% endfor %} -->
      <!-- </div> -->
      <!-- {% endif %} -->

    </main>

    <footer class="mt-10 pt-6 border-t border-gray-200 text-center text-sm text-gray-600">
      <p class="mb-2">Built with <a href="https://marimo.io" target="_blank" class="text-blue-500 hover:underline">marimo</a> - Interactive Python notebooks</p>
    </footer>
  </div>
</body>
</html>
